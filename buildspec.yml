version: 0.2

phases:
  pre_build:
    commands:
      - echo "Setting up build environment..."
  build:
    commands:
      - echo "Build phase..."
      # Add any commands here if you need to modify files before deploying
  post_build:
    commands:
      - echo "Deploying CloudFormation stack..."
      - aws cloudformation deploy --template-file cloudformation.yaml --stack-name MyStackName

